<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ITinder" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ITinder" parent="Base.Theme.ITinder" />

    <!-- Avoid white frame before Compose draws (same drawable as SplashGradientBackground). -->
    <style name="Theme.ITinder.MainScreen" parent="Theme.ITinder">
        <item name="android:windowBackground">@drawable/splash_gradient_bg</item>
        <item name="android:colorBackground">@color/gradient_bg</item>
    </style>
</resources>